# API Key de Groq (requerido — gratis en console.groq.com)
GROQ_API_KEY=tu_api_key_aqui

# Puerto del servidor (opcional, default 3001)
PORT=3001

# Rutas de datos (en producción Fly.io las sobreescribe automáticamente)
# DB_PATH=/data/clinica.db
# AUTH_PATH=/data/whatsapp-auth

# URL pública (opcional, solo para mostrar en logs)
# PUBLIC_URL=https://klin-dental.fly.dev
